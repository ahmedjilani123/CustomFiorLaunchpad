_schema-version: "3.2"
ID: sapadadvlauchpad
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: sapadadvlauchpad-destination-content
  type: com.sap.application.content
  requires:
  - name: sapadadvlauchpad-destination-service
    parameters:
      content-target: true
  - name: sapadadvlauchpad-repo-host
    parameters:
      service-key:
        name: sapadadvlauchpad-repo-host-key
  - name: sapadadvlauchpad-uaa
    parameters:
      service-key:
        name: sapadadvlauchpad-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: sapadadvlauchpad_html_repo_host
          ServiceInstanceName: sapadadvlauchpad-html5-srv
          ServiceKeyName: sapadadvlauchpad-repo-host-key
          sap.cloud.service: sapadadvlauchpad
        - Authentication: OAuth2UserTokenExchange
          Name: sapadadvlauchpad_uaa
          ServiceInstanceName: sapadadvlauchpad-xsuaa-srv
          ServiceKeyName: sapadadvlauchpad-uaa-key
          sap.cloud.service: sapadadvlauchpad
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: sapadadvlauchpad-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: sapadadvlauchpad-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sapadadvlauchpad.zip
      name: sapadadvlauchpad
      target-path: resources/
- name: sapadadvlauchpad
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: sapadadvlauchpad-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: sapadadvlauchpad-destination-service
    service-plan: lite
- name: sapadadvlauchpad-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: sapadadvlauchpad-xsuaa-srv
    service-plan: application
- name: sapadadvlauchpad-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: sapadadvlauchpad-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
